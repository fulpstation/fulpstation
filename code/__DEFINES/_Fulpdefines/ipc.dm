#define TRAIT_NO_HUSK				"no_husk" // If we have this trait, we can't get husked.
#define REVIVES_BY_HEALING				33 // Used to determine if we revive after our health reaches a certain threshold.
#define ROBOTIC_LIMBS					34 // Makes our bodyparts count as 'robotic' for surgery and the sort.
#define isIPC(A) (is_species(A, /datum/species/ipc))
